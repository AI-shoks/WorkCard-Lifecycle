# Только безопасные local demo-значения. Не используйте их в hosted environment.
APP_ENV=development
APP_VERSION=0.1.0
HOST=0.0.0.0
PORT=3000
LOG_LEVEL=info
PROXY_TRUST_MODE=none
APP_ORIGIN=http://localhost:5173
COMPOSE_APP_ORIGIN=http://localhost:3000
SESSION_SIGNING_SECRET=local-development-session-secret-change-me
DATABASE_URL=postgresql://workcard_app:workcard_app_local@localhost:55439/workcard
DEMO_MAX_BATCHES=20
DEMO_MAX_SESSIONS=500

# Owner tasks: use a separate .env.owner, never pass it to the API/browser.
# Hosted runtime additionally requires NEON_DATABASE_HOST and NEON_DATABASE_NAME.
# Render: PROXY_TRUST_MODE, PROXY_TRUSTED_CIDRS; qualify the actual peer chain.
